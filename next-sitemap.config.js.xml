/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.SITE_URL || 'https://votresite.com',
  generateRobotsTxt: true,
  robotsTxtOptions: {
    additionalSitemaps: [
      'https://votresite.com/server-sitemap.xml',
    ],
  },
  exclude: ['/admin/*'],
}